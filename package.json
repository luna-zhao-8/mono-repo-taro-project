{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "build": "lerna run build --parallel"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "jest": "^25.2.3"
  }
}
